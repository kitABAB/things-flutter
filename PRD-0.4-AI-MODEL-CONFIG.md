# Things Flutter v0.4 AI 策略组配置交互规格

> 配套高保真：`design/ai_model_config_v2.html`  
> 本文以高保真为准，说明策略组主屏、卡片内联配置、草稿保存、校验、测试和高级功能映射。

---

## 1. 核心概念

### 1.1 策略组

策略组是一组按卡片顺序调用的模型/网关模型。

```text
策略组 1
Gemini 2.5 Flash
公司 Qwen 网关 / qwen-plus
DeepSeek Chat
```

调用时只使用配置完整且启用的卡片。配置未完成的卡片会保存为草稿，但不参与调用和 fallback。

默认规则：

- 第一次配置 AI 时自动创建 `策略组 1`。
- 默认所有 AI 功能使用 `策略组 1`。
- 用户可以重命名策略组。
- 用户可以创建多个策略组。
- 所有修改实时保存，不提供显式保存按钮。

### 1.2 模型/网关卡片

策略组内只有一种可见对象：卡片。

卡片来源：

- 云模型：从内置模型目录选择，例如 Gemini、OpenAI、DeepSeek。
- 自定义网关模型：用户配置兼容网关后，从该网关添加一个或多个 Model ID。

云模型和网关模型都可以：

- 展开配置。
- 拖动排序。
- 单独测试。
- 禁用/删除。
- 配置完整后参与 fallback 调用。

卡片不展示 `主`、`备用`、`连接正常`、`未测试`、`上次失败` 这类文字。顺序由卡片位置表达，连通性由信号灯表达。

### 1.3 草稿卡片

点加号不会打开添加模态框，而是在列表末尾插入一张展开的草稿卡。

草稿卡片规则：

- 立即自动保存。
- 可以收起、退出页面、返回后继续编辑。
- 卡片上显示警告图标。
- 不参与调用。
- 不参与批量测试。
- 配置补全后自动转为生效卡片。

### 1.4 自定义网关

自定义网关是模型来源，不是独立配置页。

一个网关连接可以添加多个模型：

```text
公司 Qwen 网关
Base URL: https://llm.company.com/v1
Models:
- qwen-plus
- qwen-max
```

添加到策略组后展示为：

```text
公司 Qwen 网关 / qwen-plus
公司 Qwen 网关 / qwen-max
```

协议固定为 OpenAI Chat Completions 兼容协议，配置表单不展示协议字段。协议要求收在 `协议要求` 折叠项里。

---

## 2. 策略组主屏

对应高保真：`design/ai_model_config_v2.html`

### 2.1 页面结构

```text
[返回] 策略组 1                  [功能映射] [批量测试] [添加]

Gemini 2.5 Flash                  [闪电] [展开]
Google · 绿灯

公司 Qwen 网关 / qwen-plus         [闪电] [展开]
自定义网关 · 黄灯

选择模型                          [警告] [闪电] [展开]
云模型 · 黄灯
  云模型 / 网关
  模型选择
  API Key
  缺 API Key

[ + ]
```

主屏不展示：

- 顶部策略说明卡片。
- 当前顺序摘要。
- “模型优先级”标题。
- “拖拽排序”文字。
- 添加模型模态框。
- 底部“测试策略组”按钮。
- 底部“保存策略组”按钮。
- “添加模型或网关”文字按钮。

### 2.2 顶部操作

顶部右侧保留图标操作：

- 功能映射：进入高级功能策略配置。
- 闪电：批量测试当前策略组中配置完整且启用的卡片。
- 加号：新增一张展开的草稿卡。

### 2.3 卡片展示

每张卡片展示：

- 拖动手柄。
- 来源图标。
- 模型名称。
- 来源。
- 连接信号灯。
- 单卡闪电测试按钮。
- 展开/收起按钮。
- 未完成警告图标，仅在配置未完成时出现。

卡片底色：

- 云模型：冷白/浅蓝底。
- 自定义网关模型：浅紫底。
- 草稿/未完成：保留来源底色，同时使用琥珀色描边和警告图标。

信号灯：

- 绿灯：可用。
- 黄灯：未测或未生效。
- 红灯：失败。

主列表只显示信号灯，不显示状态文字。错误详情只在展开配置、点击红灯或测试失败反馈中出现。

### 2.4 展开与收起

点击卡片或展开按钮，卡片在原地展开。

展开状态：

- 不跳转页面。
- 不打开添加模态框。
- 配置区是卡片的一部分。
- 收起时自动保存当前内容。
- 若配置未完成，保留草稿并显示警告图标。

---

## 3. 云模型卡片配置

### 3.1 展开结构

```text
[ 云模型 ] [ 网关 ]

搜索 gpt / gemini / deepseek

Gemini 2.5 Flash
Google · 快速

DeepSeek Chat
DeepSeek · 中文理清

API Key
[ AIza••••1234 ]

缺 API Key
```

### 3.2 选择模型

模型列表在卡片内部展示，不进入新页面，不打开模态框。

模型项展示：

- 模型名称。
- 厂商。
- 极短标签，例如 `快速`、`中文理清`、`通用`。

选中后：

- 卡片标题更新为模型名称。
- 来源更新为厂商。
- 图标更新为厂商图标。
- API Key 自动带入该厂商已保存 Key。
- 触发校验并自动保存。

### 3.3 API Key 自动带入

如果选中模型所属厂商已有保存 Key，API Key 输入框直接填入脱敏值。

```text
API Key
[ AIza••••1234 ]
```

用户不再选择 radio。用户要替换时直接清空并粘贴新 Key。

### 3.4 云模型校验

云模型卡片完整条件：

- 已选择模型。
- API Key 非空。

未完成时：

- 显示警告图标。
- 展开区显示 `缺 API Key`。
- 卡片信号灯为黄灯。
- 自动保存为草稿。
- 不参与调用、fallback、批量测试。

补全后：

- 警告图标消失。
- 卡片自动生效。
- 自动保存。

---

## 4. 自定义网关卡片配置

### 4.1 展开结构

```text
[ 云模型 ] [ 网关 ]

网关名称
[ 公司 Qwen 网关 ]

Base URL
[ https://llm.company.com/v1 ]

API Key
[ 可选，本地网关可留空 ]

鉴权
[ Bearer ] [ 无 ] [ Header ]

[ 拉取 ]
协议要求 >

qwen-plus  qwen-max

缺 Base URL
缺模型
```

### 4.2 协议要求

协议要求默认折叠。

展开内容：

```text
兼容 OpenAI Chat Completions：
POST {baseUrl}/chat/completions

可选支持：
GET {baseUrl}/models
```

### 4.3 拉取模型

点击 `拉取`：

- 请求 `GET {baseUrl}/models`。
- 成功后在卡片内展示可选 Model ID。
- 失败时允许手动填写 Model ID。

### 4.4 网关校验

网关卡片完整条件：

- Base URL 非空且格式为 URL。
- 至少有一个 Model ID。
- 如果鉴权方式不是 `无`，API Key 可选但测试时要能返回有效响应。

未完成时：

- 显示警告图标。
- 展开区显示 `缺 Base URL`、`缺模型` 等校验项。
- 卡片信号灯为黄灯。
- 自动保存为草稿。
- 不参与调用、fallback、批量测试。

补全后：

- 警告图标消失。
- 卡片自动生效。
- 自动保存。

---

## 5. 添加流程

### 5.1 加号行为

点击顶部加号或列表底部加号：

- 不打开模态框。
- 在列表末尾插入一张展开的草稿卡。
- 默认类型为云模型。
- 自动保存草稿。
- 页面平滑滚动到新卡片。

### 5.2 从草稿变为生效

用户在草稿卡里完成配置后：

- 卡片标题从 `选择模型` 更新为具体模型名。
- 来源更新为厂商或自定义网关。
- 若校验通过，卡片自动生效。
- 若校验不通过，继续作为草稿保存。

### 5.3 退出页面

如果用户没有配置完就退出：

- 不弹确认。
- 保存草稿。
- 下次进入仍显示未完成卡片和警告图标。
- 该卡片不生效。

---

## 6. 测试交互

### 6.1 单卡测试

每张卡片右侧都有闪电按钮。

点击后：

- 如果配置未完成，不发起请求，提示草稿已保存。
- 如果配置完整，该按钮进入 loading 动效。
- 成功后信号灯变绿。
- 失败后信号灯变红，并可点击查看错误。
- 测试结果自动保存。

### 6.2 批量测试

顶部闪电按钮用于批量测试。

规则：

- 只测试配置完整且启用的卡片。
- 跳过草稿卡片。
- 卡片测试按钮依次进入 loading。
- 每张卡片更新自己的信号灯。
- 如果没有可用卡片，提示 `没有可用模型`。

---

## 7. 高级功能映射

### 7.1 入口

主屏顶部使用图标入口。

点击后打开 bottom sheet。

### 7.2 页面结构

```text
功能策略

智能捕获      策略组 1 >
AI 理清       理清强模型组 >
回顾建议      策略组 1 >
```

选择后立即保存。

---

## 8. fallback 调用规则

某个 AI 功能选择一个策略组后，调用该策略组中所有配置完整且启用的卡片。

会尝试下一张卡片：

- 请求超时。
- 网络异常。
- HTTP 429。
- HTTP 5xx。
- 模型返回空内容。
- JSON 解析失败，且一次修复后仍失败。

不会尝试下一张卡片：

- 用户主动取消。
- 请求参数或 prompt 构造错误。
- 明确全局断网。
- 401 / 403，且下一张卡片使用同一连接与同一 API Key。

如果下一张卡片使用不同连接或不同 Key，401 / 403 可以继续尝试。

---

## 9. 动效要求

主屏：

- 新草稿卡从列表末尾轻弹插入。
- 卡片展开/收起使用高度过渡和淡入淡出。
- 拖动中卡片浮起、轻微缩放、阴影增强。
- 放下后列表平滑补位。
- 单卡测试时闪电按钮旋转或脉冲。
- 批量测试时卡片依次进入测试状态。
- 自动保存成功时顶部状态轻微闪一下。

高级配置：

- 功能映射使用 bottom sheet。
- 选择策略组后对应功能行短暂高亮。

---

## 10. 验收标准

- 添加模型不再打开模态框。
- 点击加号直接新增一张展开的草稿卡。
- 每张模型/网关卡片都可以在原地展开配置。
- 草稿卡可以收起和退出后继续保存。
- 配置未完成的卡片有警告图标。
- 配置未完成的卡片不参与调用、fallback、批量测试。
- 云模型配置在卡片内完成模型选择和 API Key 填写。
- 云模型已保存 Key 会直接自动带入输入框。
- 云模型不出现“使用已保存 Key / 粘贴新 Key”的 radio。
- 自定义网关配置在卡片内完成 Base URL、API Key、鉴权和模型选择。
- 自定义网关协议要求默认折叠。
- 主列表不显示 `主`、`备用`、连接状态文字。
- 主列表连接状态只显示红/绿/黄信号灯。
- 云模型和自定义网关模型用不同卡片底色区分。
- 所有修改实时保存。
- 高保真体现内联展开、草稿保存、校验提示、拖拽卡片、单卡测试、批量测试、自动保存反馈。
